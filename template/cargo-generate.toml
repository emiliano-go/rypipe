[template]
cargo_generate_version = ">=0.10.0"
description = "A rypipe adapter crate with Python bindings"

[placeholders.description]
type = "string"
prompt = "Adapter description"
default = "A rypipe adapter for key=value records"
